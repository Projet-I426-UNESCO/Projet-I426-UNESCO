<link rel="stylesheet"
  href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@23,400,0,0&icon_names=arrow_back" />
@layout({
title: t('messages.auth.login_title'),
hideHeader: true
})
<div class="goback">
  <a href="{{ route('home') }}" class="link">
    <div class="goback-link">
      <span class="material-symbols-outlined" height="20" width="20">
        arrow_back
      </span>
      <p>
        {{ t('messages.auth.back') }}</p>
    </div>
  </a>
</div>
<div class="auth-page-container">
  <div class="auth-card">
    <h1>{{ t('messages.auth.login_title') }}</h1>

    <form method="POST" action="{{ route('auth.login.store') }}">
      {{ csrfField() }}

      <div class="input-group">
        <input type="text" name="fullName" value="{{ old('fullName') || '' }}"
          placeholder="{{ t('messages.auth.username_placeholder') }}" required />
        @inputError('fullName')
        <span class="error-text">{{ $messages.join(', ') }}</span>
        @end
      </div>

      <div class="input-group">
        <div class="password-wrapper">
          <input type="password" name="password" placeholder="{{ t('messages.auth.password_placeholder') }}" required />
        </div>
        @inputError('password')
        <span class="error-text">{{ $messages.join(', ') }}</span>
        @end
      </div>


      <button type="submit" class="btn-primary">{{ t('messages.auth.login_button') }}</button>

      @error('E_INVALID_CREDENTIALS')
      <p class="error-message">{{ t('messages.auth.invalid_credentials') }}</p>
      @end
    </form>

    <div class="auth-footer">
      <span>{{ t('messages.auth.no_account') }}</span>
      <a href="/register">{{ t('messages.auth.register_button') }}</a>
    </div>
  </div>
</div>
@end

<style>
  body {
    display: flex;
    flex-direction: column;
  }

  .goback {
    margin: 20px;
  }

  .goback .link {
    color: #c2c2c2;
    text-decoration: none;
  }

  .goback-link p {
    font-weight: 400
  }

  .goback-link {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .auth-page-container {
    display: grid;
    place-items: center;
    flex: 1;
    margin-bottom: 100px;

  }

  form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .auth-card {
    background: rgb(235, 235, 235);
    padding: 2rem;
    border-radius: 7px;
    display: flex;
    flex-direction: column;
    gap: 25px;
  }

  input {
    padding: 12px;
    border: 1px solid #ddd;
    border-radius: 8px;
    font-size: 16px;
    width: 350px;
    background-color: rgb(245, 245, 245);
  }

  .btn {
    background: #6366f1;
    color: white;
    border: none;
    padding: 12px;
    border-radius: 25px;
    font-weight: bold;
    cursor: pointer;
  }

  .link {
    color: #6366f1;
    text-decoration: none;
    font-weight: 600;
  }

  h1 {
    margin: 0;
    font-size: 1.5rem;
  }
</style>